Claude Prompt (Terraform + AgentCore + API Gateway + MCP) — copy/paste

You are an expert AWS Solutions Architect and full-stack engineer. Build me a complete, copy/paste-ready project called:

Serverless MCP Chatbot Demo

This is for an AWS User Group live demo. It must be cheap, minimal, reliable, and explainable in under 5 minutes.

Hard requirement

Every AWS resource must be tagged with:

project = "serverless-mcp-chatbot-demo"

This includes S3 buckets/objects (where supported), Lambda, API Gateway, IAM roles/policies (where supported), logs, etc. Use provider default_tags and/or explicit tags to guarantee coverage.

High-level goal

A tiny web chatbot UI hosted in S3 calls an API Gateway endpoint. The backend uses AgentCore to run an “agent” that can call an MCP tool (read_doc) implemented via FastMCP. The MCP tool reads a small handbook.txt from S3 and returns it. The agent uses that to answer questions.

The chatbot should visibly demonstrate “agentic/tool behavior” by invoking the tool when the user asks about the handbook.

Architecture requirements (strict)
1) Frontend

Single-page index.html + minimal inline JS/CSS (no frameworks)

Hosted from an S3 bucket (static website hosting is fine)

UI: transcript, input, send button

Calls backend: POST https://<api-gateway-url>/chat

Shows:

assistant reply

small “tool used” indicator if backend reports it

2) API Layer

Use API Gateway HTTP API (preferred for simplicity/cost) unless you must use REST API for a required feature.

Route: POST /chat

Integration: Lambda proxy integration

Enable CORS for the S3 website origin

3) Compute

Lambda (Python 3.12)

Lambda receives { "message": "..." }

Lambda calls AgentCore to run the agent conversation and returns { "reply": "...", "tool_used": true/false }

4) Agent layer (AgentCore)

Use AgentCore as the orchestrator:

Define/configure an agent that can use one tool called read_doc

The agent should:

answer normally when possible

call read_doc ONLY when the user asks about handbook content or needs verification from it

keep answers short and demo-friendly

⚠️ Important: If AgentCore requires specific resource setup (agent definitions, policies, tool registration, runtime config, etc.), include it in Terraform (or in a clear “one-time CLI setup” section if Terraform cannot create it).

If AgentCore details are ambiguous, do the following:

First, implement the “AgentCore path” as the primary design.

Also include a “fallback mode” in code that uses Bedrock tool use via Converse directly (without AgentCore) so the demo still works. Keep fallback minimal and clearly separated.

5) MCP Tooling

Implement an MCP server using FastMCP (Python)

Expose exactly one tool:

read_doc(bucket: str, key: str, max_chars: int = 8000) -> str

The tool reads from S3 via boto3 and returns text (truncate to max_chars)

Run MCP server as an HTTP service for the demo:

Primary: run locally on laptop for the meetup (HTTP transport)

Optional: describe how to deploy it later (not required)

Lambda calls the MCP server using env var MCP_SERVER_URL.

6) S3 Document

Include /handbook/handbook.txt in repo with obvious content (RTO/RPO, resiliency bullets) so tool use is easy to demonstrate.

Store it in S3 (same bucket or separate bucket). Minimal is fine.

Terraform requirements (important)

Use Terraform with AWS provider to create:

Resources

S3 bucket for frontend hosting + upload index.html (and optional config file)

S3 bucket/object for handbook.txt

Lambda function (zip deployment)

API Gateway HTTP API with route POST /chat

Lambda permission for API Gateway invocation

CloudWatch log group (tagged) with retention (e.g., 7 days) to keep costs down

IAM

Lambda can s3:GetObject for handbook object

Lambda can call AgentCore and/or Bedrock (least privilege possible)

If AgentCore requires additional permissions/policies, include them.

Tagging (must)

Use provider default_tags:

project = "serverless-mcp-chatbot-demo"

Also explicitly tag resources that don’t inherit default tags reliably.

Outputs

API Gateway invoke URL

Frontend website URL / bucket name

Any relevant IDs needed for demo

Files

Keep Terraform minimal and readable:

/infra/terraform/versions.tf

/infra/terraform/variables.tf

/infra/terraform/main.tf

/infra/terraform/outputs.tf

Lambda behavior requirements

Endpoint: POST /chat

Input JSON: { "message": "..." }

Output JSON:
{ "reply": "...", "tool_used": true/false, "mode": "agentcore" | "fallback" }

Handle CORS

Validate input, handle errors gracefully

Set timeouts and keep it snappy

Never leak secrets in responses

Lambda env vars:

PROJECT_TAG = serverless-mcp-chatbot-demo (used in logs)

ALLOWED_ORIGIN (S3 website origin)

DOC_BUCKET

DOC_KEY

MCP_SERVER_URL

Any AgentCore config vars required (agent ID/name, region, etc.)

Fallback vars if needed:

BEDROCK_MODEL_ID

Project layout to generate (must match)
/infra/terraform/

versions.tf

variables.tf

main.tf

outputs.tf

/lambda/

handler.py

requirements.txt

build.sh (install deps into a package dir and zip)

README.md (how to build zip)

/mcp_server/

server.py (FastMCP HTTP server exposing read_doc)

requirements.txt

run.sh

/frontend/

index.html

Frontend should read API URL from either:

a constant placeholder const API_URL = "https://YOUR_API_URL_HERE/chat";

OR a config.js if present

/handbook/

handbook.txt

Root /README.md

Provide full steps:

prerequisites (AWS CLI configured, Terraform, Python, Node optional)

start MCP server locally

build lambda zip

terraform apply

update frontend with API URL and upload

test prompts

Include troubleshooting:

CORS (S3 origin vs API Gateway)

API Gateway integration errors

AgentCore permissions/setup issues

fallback mode usage

tool not triggering (prompt tuning tips)

Demo prompts (include in README)

triggers tool:
“Summarize the handbook in 3 bullets”

triggers tool:
“What does the handbook say about RTO/RPO?”

does NOT trigger tool:
“What is MCP?”

Tool usage must be visible:

backend returns tool_used: true when tool executed

frontend shows “tool used” indicator

Constraints

Keep it minimal and cheap

No frontend frameworks

No DynamoDB

No LangChain

Use API Gateway (as required)

Use AgentCore (primary) with a fallback that still works if AgentCore setup differs

Extra: 30-second speaker script

At end of README include a short talk track:
“Website → API Gateway → Lambda → AgentCore → MCP tool → S3 → answer”

Now generate the entire repo with all files and code blocks.


i also want gitignore. the readme should explain this is intended to be used as a demo in aws user groups and at universities. include a license that allows people to steal the code like mit. 

explain each compoent in the readme in a way that a college student will be able to understand. 

make sure cognito only allows .lukelittle.com email addresses and make a point to point this out in the readme so that people will know to undo this 


